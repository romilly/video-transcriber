# Progress Report - 2025-12-28

## Session Summary

Added audio-only transcription mode and documentation cleanup.

## Completed

### Audio-Only Mode
- Added `audio_only` parameter to `transcribe_video()` function
- Added `extract_frames` parameter to `VideoTranscriber.process_video()`
- Updated `ZipMarkdownReportGenerator` to produce clean audio-only output (no "No frames extracted" message)
- When `audio_only=True`, skips frame extraction entirely and outputs just timestamped transcript
- Added 2 new unit tests for the feature

### Documentation Cleanup
- Added audio_only option to README with dedicated "Audio-Only Mode" section
- Removed outdated "Running the Code" section from CLAUDE.md (spike notebook reference, non-existent CLI)
- Removed duplicate "Installation & Setup" section from CLAUDE.md (already in README)
- Updated CLAUDE.md Configuration Options to document all `transcribe_video` and `VideoTranscriber` parameters
- Simplified README Hugging Face section

### Test Status
- All 28 tests pass (11 integration + 17 unit)

### v0.3 Release
- Built wheel and sdist
- Uploaded to PyPI: https://pypi.org/project/video-transcriber/0.3/
