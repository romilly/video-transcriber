# Progress Report - 2025-12-18

## Session Summary

Focused on packaging cleanup and test improvements.

## Completed

### Packaging Updates
- Added missing `numpy` to pyproject.toml dependencies
- Removed unused `requests` and `python-dotenv` from requirements.txt
- Created separate `requirements-test.txt` for test/dev dependencies
- Fixed deprecated license format (changed to SPDX expression `"MIT"`)
- Removed deprecated license classifier from pyproject.toml
- Deleted redundant `setup.cfg` (pyproject.toml handles everything)
- Added `build` tool to dev dependencies
- Exported `transcribe_video` from package `__init__.py`
- Updated `__version__` to `"0.1.1"`

### Wheel Build Verification
- Successfully built `video_transcriber-0.1.1-py3-none-any.whl`
- Tested installation in fresh venv - works correctly

### Test Cleanup
- Removed all `skipif` decorators from `tests/integration/test_audio_adapters.py`
- Removed `skipif` decorator from `tests/integration/test_opencv_adapter.py`
- Updated CLAUDE.md with guideline: "Never skip tests due to configuration issues"
- Updated all integration tests to use `tests/data/demo.mp4` instead of files in `data/`
- All 26 tests pass (11 integration + 15 unit)

### Git Cleanup
- Removed large files from git history
- Force-pushed to clean up remote

## Pending / Next Steps

### Uncommitted Changes
- Changes from this session need to be committed:
  - `tests/integration/test_audio_adapters.py` - use demo.mp4, removed skipif
  - `tests/integration/test_opencv_adapter.py` - use demo.mp4, removed skipif
  - `CLAUDE.md` - added test skip guideline
