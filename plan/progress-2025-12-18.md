# Progress Report - 2025-12-18

## Session Summary

Test cleanup, packaging improvements, and v0.2.0 release on PyPI.

## Completed

### Test Cleanup
- Removed all `skipif` decorators from integration tests
- Tests now fail (not skip) when configuration is wrong
- Updated CLAUDE.md with guideline: "Never skip tests due to configuration issues"
- Updated all integration tests to use `tests/data/demo.mp4` (tracked in repo)
- Fixed `.gitignore` typo: `test/test/data/generated` -> `tests/data/generated/`
- All 26 tests pass (11 integration + 15 unit)

### Demo Script Updates
- Updated `demo_create_zip.py` to use `tests/data/demo.mp4`
- Output now goes to `tests/data/generated/demo.zip`

### v0.2.0 Release
- Bumped version to 0.2.0 in `pyproject.toml` and `__init__.py`
- Fixed GitHub URLs in `pyproject.toml` (was placeholder `username`)
- Built wheel and sdist
- Uploaded to PyPI: https://pypi.org/project/video-transcriber/0.2.0/

### README Updates
- Installation now uses `uv venv` and `uv pip install video-transcriber`
- Added Windows instructions throughout:
  - ffmpeg: `winget install ffmpeg`
  - venv activation: `.venv\Scripts\activate`
  - HF_TOKEN: `set HF_TOKEN=...`
- Moved Quick Demo to Development section
- Added full clone/setup instructions for developers

## All Changes Committed and Pushed

No pending work.
